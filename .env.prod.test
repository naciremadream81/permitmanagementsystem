# Test Production Environment Configuration
# This is for testing the production setup locally

# Database Configuration
POSTGRES_DB=permit_management_prod_test
POSTGRES_USER=permit_user
POSTGRES_PASSWORD=test-secure-password-123

# JWT Configuration (32+ characters required)
JWT_SECRET=test-super-secure-jwt-secret-key-for-production-testing-123

# Server Configuration
SERVER_PORT=8080
SERVER_HOST=0.0.0.0
ENVIRONMENT=production
LOG_LEVEL=INFO

# File Upload Configuration
UPLOAD_MAX_SIZE=10485760
UPLOAD_DIR=/app/uploads

# CORS Configuration
CORS_ALLOWED_ORIGINS=https://localhost,http://localhost:3000

# Domain Configuration
DOMAIN=localhost
SSL_EMAIL=admin@localhost

# Backup Configuration
BACKUP_ENABLED=true
BACKUP_RETENTION_DAYS=30
